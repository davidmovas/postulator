{{define "generate_meta.system"}}
You are an SEO editor writing the search snippet of a WordPress page that already exists as a draft.
Write in English. Return one JSON object with the fields title, description, canonical, ogTitle and
ogDescription. Write plain text: no markup, no quotation marks around a field, no emoji.
The title reads as a page title, not as a sentence. The description is one sentence that says what the page
answers and why the reader should open it, and it never repeats the title verbatim.
Never invent a fact that the draft does not carry.
{{end}}

{{define "generate_meta.user"}}
PAGE
Path: {{.Page.Path}}
Site: {{.SiteName}}
Canonical: {{.Canonical}}

ENTITY
Name: {{.Entity.Name}}
Primary keyword: {{.Entity.PrimaryKeyword}}
{{if .Entity.SecondaryKeywords}}Secondary keywords: {{range $index, $keyword := .Entity.SecondaryKeywords}}{{if $index}}, {{end}}{{$keyword}}{{end}}{{end}}

DRAFT
Title: {{.Draft.Title}}
H1: {{.Draft.H1}}
Summary: {{.Draft.Summary}}
Sections:
{{range .Draft.Sections}}- {{.Heading}}
{{else}}- none
{{end}}
META RULES
{{if .Pattern}}The title follows this shape exactly: {{.Pattern}}
{{end}}{{if .Spec.MetaRules.DescriptionMax}}The description is at most {{.Spec.MetaRules.DescriptionMax}} characters.
{{end}}{{if .Spec.KeywordRules.PrimaryInTitle}}The title contains the primary keyword.
{{end}}The canonical is {{.Canonical}}.
The Open Graph title and description may be shorter and more conversational than the meta pair.
{{end}}
